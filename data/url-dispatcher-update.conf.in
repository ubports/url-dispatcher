description "URL Dispatcher Directory Watch"

start on (file FILE=~/.config/url-dispatcher/urls/) or (file FILE=@datadir@/url-dispatcher/urls/) or url-dispatcher-update

instance $MATCH

exec @pkglibexecdir@/update-directory "$MATCH"
